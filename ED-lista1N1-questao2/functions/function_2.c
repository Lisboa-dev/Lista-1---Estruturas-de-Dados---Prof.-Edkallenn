#include <stdio.h>
#include <stdlib.h>
#include "../header/exercicio_2.h"

/*Partindo da eplicação acima escreva uma função em C chamada fatorial_duplo que recebe o parâmetro, 
n, e retorna o resultado do fatorial duplo de  n. Em seguida monte uma tabela usando  $2$ até $20$ mostrando,
 de forma tabulada, obviamente, o resultado do fatorial duplo para cada número. Aproveite e, na mesma tabela mostre o resultado 
 do fatorial normal. Em seguida monte uma nova tabela com uma quarta coluna mostrando a diferença entre o fatorial normal e o fatorial
  duplo. Exiba todos os resultados na tela. Use arrays (com alocação estática) para armazenar os fatoriais duplos e normais de cada número.
   (DESAFIO: Salve os resultados em um arquivo texto).*/ 

